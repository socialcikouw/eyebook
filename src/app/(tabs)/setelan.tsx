import MenuSetelan from "@/src/components/common/MenuSetelan";
import React from "react";
import { ScrollView } from "react-native";

export default function setelan() {
  return (
    <ScrollView>
      <MenuSetelan />
    </ScrollView>
  );
}
